let bootDbConnection,bootEnvConfig,routes;_68c‍.w("./bootstrap",[["bootDbConnection",["bootDbConnection"],function(v){bootDbConnection=v}]]);_68c‍.w("./config",[["bootEnvConfig",["bootEnvConfig"],function(v){bootEnvConfig=v}]]);_68c‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);var createError = require('http-errors');
var express = require('express');
var path = require('path');
var cors = require('cors');
var logger = require('morgan');
var app = express();




const port = 8081;

app.use(cors());
app.use(logger('dev'));
app.use(express.json());
app.use(express.urlencoded({extended: false}));
app.use(express.static(path.join(__dirname, 'public')));


bootDbConnection();
_68c‍.g.console.log(bootEnvConfig());

app.use('/api', routes)

// catch 404 and forward to error handler
app.use(function (req, res, next) {
    next(createError(404));
});

// error handler
app.use(function (err, req, res, next) {
    // set locals, only providing error in development
    res.locals.message = err.message;
    res.locals.error = req.app.get('env') === 'development' ? err : {};

    // render the error page
    res.status(err.status || 500);
    res.render('error');
});

app.use('/', function (req, res) {
    res.json({serverStatus: 'running'});
})

const request = app.listen(port, () => _68c‍.g.console.log(`Example app listening on port ${port}!`))
//request.timeout = 2000;
module.exports = app;